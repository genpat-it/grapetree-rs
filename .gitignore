/target
# stray temp files (GrapeTree's oracle writes tmp*/*.npy into the cwd)
tmp*
*.npy
*.list
*.nwk
_r[dt]/

# Private internal analysis — never publish (not a public critique of upstream)
WEAKNESS.md
